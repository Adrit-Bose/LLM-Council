/** Card reveal stagger (ms between each member card resolving) */
export const CARD_STAGGER_MS = 150;

/** Delay before inner card content (lists) fades in after card shell */
export const CARD_CONTENT_DELAY_MS = 80;

/** Chairman report section stagger */
export const CHAIRMAN_SECTION_STAGGER_MS = 150;

/** Score count-up and bar width animation duration */
export const COUNT_UP_MS = 600;

/** Debate transitional pulse duration before cross-fade */
export const DEBATE_TRANSITION_MS = 1200;

/** Total chairman report sections to reveal in sequence */
export const CHAIRMAN_SECTION_COUNT = 5;
