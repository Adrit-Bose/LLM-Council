# LLM Council — environment configuration
# Copy to .env.local and fill in your values.

# ── Supabase (auth + history) ─────────────────────────────────────────────────
# From Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://dusobvdlxqluubbzkbeg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ZGP6YE6rUuwgtMpHzuFIEQ_ntNJw129

# Enable Google OAuth in Supabase Dashboard → Authentication → Providers → Google.
# Add redirect URL: http://localhost:3000/auth/callback (and your production URL).

# ── LLM providers (session-only in browser) ───────────────────────────────────
# Users enter API keys in the UI — never store them here or in Postgres.

# Optional global overrides for custom OpenAI-compatible providers
# DEFAULT_MODEL=gpt-4o-mini
# CHAIRMAN_MODEL=gpt-4o
# OPENAI_BASE_URL=https://api.openai.com/v1

# Per-member model overrides
# SKEPTIC_MODEL=gpt-4o-mini
# MARKET_ANALYST_MODEL=gpt-4o-mini
# TECHNOLOGIST_MODEL=gpt-4o-mini
# VISIONARY_MODEL=gpt-4o-mini
# DEVILS_ADVOCATE_MODEL=gpt-4o-mini
# PRAGMATIST_MODEL=gpt-4o-mini
